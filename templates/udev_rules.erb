# MANAGED BY PUPPET (network/templates, via params/hiera)
<% @network_interfaces.each do |ifname, data| -%>
SUBSYSTEM=="net", ACTION=="add", DRIVERS=="?*", ATTR{address}=="<%= data['mac'] %>" NAME="<%= ifname %>"
<% end %>
